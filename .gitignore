# OS junk
.DS_Store

# Dependencies & build output (Phase 0+)
node_modules/
.next/
dist/

# Local database (progress data — back up separately); root-anchored so
# dataset dirs like pascal-assmt-refs/RMP/data/ stay tracked
/data/

# Env & secrets
.env
.env.*

# Next.js / Node (merged from create-next-app)
/node_modules
/.next/
/out/
/build
/coverage
*.pem
npm-debug.log*
*.tsbuildinfo
next-env.d.ts
.vercel

# Pyodide assets copied from node_modules by scripts/copy-pyodide.mjs
/public/pyodide/
.next-verify/
